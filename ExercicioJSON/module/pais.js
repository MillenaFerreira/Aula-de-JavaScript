var estados = require('../json/estados_cidades.js');

//Data: 03/03/22
//Criar uma função (getListaDeEstados) que retorna a lista de todos os estados do Brasil. 
console.log(estados.estadosCidades);

const getListaDeEstados = function(){
    //1º Criar um json
    

    //criar um array que vai conter as siglas, guardar dentro de uma array com o nome uf

    //mostrar a quantidade de itens que tem na array(quantidade vai estar dentro do json e não dentro do array)
}